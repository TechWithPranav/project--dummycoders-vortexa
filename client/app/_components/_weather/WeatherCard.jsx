// import { SunIcon, CloudIcon, CloudRainIcon, CloudWindIcon } from "@heroicons/react/solid"; // Example icons

// function WeatherCard({ message }) {
//   const getIconForMessage = (msg) => {
//     if (msg.includes("sun") || msg.includes("UV")) return SunIcon;
//     if (msg.includes("rain") || msg.includes("umbrella")) return CloudRainIcon; // Use CloudRainIcon for rain
//     if (msg.includes("wind")) return CloudWindIcon; // Use CloudWindIcon for wind
//     return CloudIcon; // Default icon
//   };

//   const Icon = getIconForMessage(message);

//   return (
//     <div className="flex items-center p-4 bg-white shadow rounded-lg">
//       <Icon className="h-10 w-10 text-teal-500 mr-4" />
//       <p className="text-gray-700">{message}</p>
//     </div>
//   );
// }

// export default WeatherCard;
